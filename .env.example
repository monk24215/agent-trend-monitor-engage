# Copy this file to ".env" (same folder as run_tmea.bat) and paste your
# real key in. ".env" is already in .gitignore, so it never gets committed.
#
# Without this, run_tmea.bat still works — it just runs the pipeline in
# mock mode (no real model calls) instead of failing.
ANTHROPIC_API_KEY=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